{
  "name": "wartab",
  "version": "0.2.1",
  "private": true,
  "description": "Self-hosted new tab page",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "vitest": "^3.1.0"
  }
}
